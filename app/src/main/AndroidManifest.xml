<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devhub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DevHub">
        <activity android:name=".Activities.ProfilePage"></activity>
        <activity android:name=".Activities.AllNotifications" />
        <activity android:name=".Activities.ImageCommentPage" />
        <activity android:name=".Activities.TextCommentPage" />
        <activity android:name=".Activities.AddThoughtsPage" />
        <activity android:name=".Activities.MainContentPage" />
        <activity android:name=".Activities.RegisterPage" />
        <activity android:name=".Activities.LoginPage" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>